const initialState = {
  userInfor: {
    name: "Dat Nguyen",
    title: "FRONT_END",
  },
  isLoading: false,
  error: {},
};

export function sideBarReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
